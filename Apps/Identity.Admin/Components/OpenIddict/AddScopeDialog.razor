<MudDialog>
    <DialogContent>
        <MudGrid Spacing="3" Justify="Justify.FlexStart">
            <MudItem xs="10" Style="width:360px">
                 <MudAutocomplete T="ScopeSummaryDto" Label="Scopes" @bind-Value="_selectedOption" SearchFunc="@SearchScopes"
                         ResetValueOnEmptyText="true" ToStringFunc="@(e=> e == null ? null : e.DisplayName)"
                         CoerceText="false" CoerceValue="false" Class="mt-n3" Variant="Variant.Text" 
                         Margin="Margin.None" Immediate="true"
                         AdornmentIcon="@Icons.Material.Filled.Search" AdornmentColor="Color.Primary" />
            </MudItem>
            <MudItem xs="2">
                <MudIconButton UserAttributes="@(new (){{"id","btnAddScope"}})" OnClick="AddScope" Disabled="@(null == _selectedOption)"
                               Icon="@Icons.Material.Filled.Add" Color="Color.Primary" />
            </MudItem>
        </MudGrid>     
    </DialogContent>
</MudDialog>
