<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            Add new claim
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudTextField id="txtClaimType" @bind-Value="@_model.Type" Label="Type"  @onfocus="(() => _error = string.Empty)"
                      Variant="Variant.Text" Immediate="true"></MudTextField>
        <MudTextField id="txtClaimValue" @bind-Value="@_model.Value" Label="Value" @onfocus="(() => _error = string.Empty)"
                      Variant="Variant.Text" Immediate="true"></MudTextField>
        <br/>
        <MudSwitch id="cbIncludeInAccessToken" @bind-Value="_model.IncludeInAccessToken" Label="Include in access token" Color="Color.Primary" />

        <MudSwitch id="cbIncludeInIdentityToken" @bind-Value="_model.IncludeInIdentityToken" Label="Include in identity token" Color="Color.Primary" />

        @if (!string.IsNullOrEmpty(_error))
        {
            <MudAlert id="errorMessage" Severity="Severity.Error">@_error</MudAlert>
        }
    </DialogContent>
    <DialogActions>
        <MudButton id="btnAddNewClaim" OnClick="AddNewClaimAsync" 
                Disabled="string.IsNullOrEmpty(_model.Type) || string.IsNullOrEmpty(_model.Value)"
                Color="Color.Primary" Variant="Variant.Filled">
                Add
        </MudButton>
    </DialogActions>
</MudDialog>
